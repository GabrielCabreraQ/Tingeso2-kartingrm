spring:
  application:
    name: SpecialDays-service
  cloud:
    config:
      enable: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/specialdays
    user: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update